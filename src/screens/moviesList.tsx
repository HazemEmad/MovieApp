import React, {FC} from 'react';
import styled from 'styled-components/native';

const MovieList: FC = () => {
  return <Text>MovieList</Text>;
};
const Text = styled.Text`
  color: red;
`;
export default MovieList;
